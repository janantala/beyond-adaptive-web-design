This command:

	pdflatex test

builds a very simple document `test.pdf'.
-------------------------------------------------------------------------------
These commands:

	pdflatex example
	bibtex example
	pdflatex example
	pdflatex example

build a larger document `example.pdf'.
-------------------------------------------------------------------------------
These commands:

	pdflatex extended-abstract
	bibtex extended-abstract
	pdflatex extended-abstract
	pdflatex extended-abstract

build `extended-abstract.pdf'.
